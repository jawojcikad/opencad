{
  "name": "opencad",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source Electronic Design Automation tool",
  "scripts": {
    "dev": "pnpm --filter @opencad/app dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  }
}
